#!/usr/bin/env bash
# This script lists non-hidden files and prints only the part after the first dash in each filename

for file in *-*
do
  name_after_dash="${file#*-}"
  echo "$name_after_dash"
done
